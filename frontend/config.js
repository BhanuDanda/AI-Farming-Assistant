window.KRISHI_AI_CONFIG = {
  // Set this to your deployed backend URL if you have one.
  apiBaseUrl: '',

  // false = prefer direct Gemini calls from browser (GitHub Pages friendly)
  // true  = prefer backend endpoints first
  preferBackend: false,

  // Gemini model used for direct browser calls
  geminiModel: 'gemini-2.5-flash'
};
